# backtracking N Queens

Install packages: 
```bash
npm i
```
and open the `index.html` in the browser or use the live server config in `.vscode/launch.json` 

## dev 
Run the Ts compiler (optional) 
```bash
npx tsc --watch
```
